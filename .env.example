# JWT Configuration
# For HS256 (symmetric) - Simple secret key
JWT_SECRET=your-secret-key-change-in-production

# JWKS Endpoint for AppDev tokens (RS256)
# This endpoint provides the public keys for JWT verification
JWKS_ENDPOINT=https://dev-placenet.fra1.cdn.digitaloceanspaces.com/dev-jwks.json

# Database path
DB_PATH=./data/control_horario.db

# Server port (optional, default 5173 in dev)
PORT=3000

# Environment
NODE_ENV=development
